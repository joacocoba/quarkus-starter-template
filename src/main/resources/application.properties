# Application Configuration
quarkus.application.name=quarkus-starter-template
quarkus.application.version=1.0.0-SNAPSHOT

# HTTP Configuration - Centralized port configuration
%prod.quarkus.http.port=${PORT:8080}
%dev.quarkus.http.port=${DEV_PORT:8080}
%test.quarkus.http.port=${TEST_PORT:0}
quarkus.http.host=localhost
quarkus.http.cors=true

# Repository Configuration - Environment variable support
app.repository.type=${REPOSITORY_TYPE:in-memory}

# OpenAPI Configuration - Dynamic server URL based on profile
%prod.quarkus.smallrye-openapi.servers=${API_BASE_URL:http://localhost:${PORT:8080}}
%dev.quarkus.smallrye-openapi.servers=http://localhost:${DEV_PORT:8080}
%test.quarkus.smallrye-openapi.servers=http://localhost:${TEST_PORT:0}

# OpenAPI Information
quarkus.smallrye-openapi.info-title=Quarkus Starter Template API
quarkus.smallrye-openapi.info-version=1.0.0
quarkus.smallrye-openapi.info-description=Production-ready Quarkus starter template with hexagonal architecture, transaction management example, and comprehensive API features
quarkus.smallrye-openapi.info-contact-email=support@example.com
quarkus.smallrye-openapi.info-license-name=MIT

# Health Check Configuration
quarkus.smallrye-health.root-path=/q/health

# Logging Configuration
quarkus.log.level=INFO
quarkus.log.console.enable=true
quarkus.log.console.format=%d{yyyy-MM-dd HH:mm:ss,SSS} %-5p [%c{1}] (%t) %s%e%n

# Validation Configuration
quarkus.hibernate-validator.fail-fast=false
