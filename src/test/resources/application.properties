# Minimal Test Configuration for Quarkus 3.20.0
app.repository.type=in-memory

# Logging
quarkus.log.level=WARN
quarkus.log.category."com.example.transactions".level=INFO

# HTTP - Use port 0 for random available port
quarkus.http.test-port=0

# Disable all dev services to prevent hanging
quarkus.devservices.enabled=false
